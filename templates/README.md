# Delivery Templates

Manifest-registered canonical Mode B template:

`templates/Procreate/Procreate新笔刷绘画操作流程_V2.xlsx`

Rules:
- Runtime only loads this workbook at the delivery stage, after the final/provisional native brush family has been resolved and built.
- Generated project XLSX must reference real delivered native brush names.
- `KB-MANIFEST.json` is the only router for canonical template paths.
- No other workbook in this directory is canonical.
